package Model.Fields;

public class Start extends Field{
    /**
     *
     * @param name
     * @param message
     */
    public Start(String name, String message) {
        super(name, message);
    }
}